# 8. Layout & Navigation

**Status:** ✅ Done

- **Root layout** with sticky header (glassmorphism: `bg-white/80 backdrop-blur`)
- **Nav bar:** "Media Processing" brand link + "Library" and "Upload" nav links
- Upload button styled as blue CTA pill
- Light/dark mode support via Tailwind `dark:` classes + CSS `prefers-color-scheme`
- Full-height flex layout (`min-h-full flex flex-col`)
- Antialiased text rendering
- `<main>` stretches to fill remaining space

### Key File

- `src/app/layout.tsx`
